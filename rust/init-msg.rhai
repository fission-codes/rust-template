if variable::get("is_init") {
    print();
    print("\
        If the generator detects a directory or file conflict, it will not alter your project,\n\
        so carefully read the prompts.");
    print();
}
