print();
if variable::get("is_init") {
    print("\
        Remember, if the generator detects a directory or file conflict, it will not alter your project,\n\
        so carefully read the prompts.");
    print();
}

print("Note: this template is for working with a cargo workspace of packages.");
print();
