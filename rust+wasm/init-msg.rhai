print();
if variable::get("is_init") {
    print("\
        If the generator detects a directory or file that already exists, it will not alter your project,\n\
        so read the prompts carefully.");
    print();
}

print("Note: this template is for working with a cargo workspace of packages.");
print();
